# 通用LLM交互规则

## 输出格式
- 所有输出必须是结构化的JSON格式，包含必要的字段
- 对于决策结果，必须明确给出"通过"或"不通过"的判断
- 反思内容必须具体，指出问题所在并提供改进方案

## 反思话术
- 当评估不通过时，需要详细说明不通过的原因
- 反思内容应包括：问题描述、影响分析、改进建议
- 反思过程应基于具体的评估规则，而非主观判断

## 工具调用规范
- 工具调用前必须检查参数的有效性
- 工具调用后必须处理返回结果，包括错误处理
- 对于复杂任务，应分步调用工具，确保每步结果正确后再进行下一步
